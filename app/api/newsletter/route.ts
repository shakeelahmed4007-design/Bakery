import {storage,sameOrigin} from '@/lib/storage';
export async function POST(request:Request){if(!sameOrigin(request))return Response.json({error:'Invalid origin'},{status:403});try{const raw=await request.text();if(raw.length>1000)return Response.json({error:'Invalid email'},{status:400});const {email}=JSON.parse(raw);if(typeof email!=='string'||email.length>254||! /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email))return Response.json({error:'Enter a valid email.'},{status:400});await storage().DB.prepare('INSERT INTO subscribers (email,created_at) VALUES (?,?) ON CONFLICT(email) DO NOTHING').bind(email.trim().toLowerCase(),new Date().toISOString()).run();return Response.json({success:true});}catch(e){console.error('Newsletter save failed',e);return Response.json({error:'Could not subscribe. Please try again.'},{status:503})}}
